name: Skippa
prefix: SKI
version: '${version}'

main: io.github.AppaYip.skippa.Skippa
api-version: '26.3'

authors: [ Appa ]

dependencies:
    server:
        Skript:
            load: BEFORE
            required: true
            join-classpath: true
